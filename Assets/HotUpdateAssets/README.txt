HybridCLR generated DLLs and supplementary AOT metadata are copied into this
YooAsset collector folder by the commercial build pipeline. The algorithm
sandbox itself stays independent from these packaged assets.
